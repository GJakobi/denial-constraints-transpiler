# Test DCs with known violations
# Dataset: employees (6 rows)
#
# DC 1: no two employees can have the same salary
#   Expected violations: pairs {1,2} and {4,5}
¬(t0.employees.salary == t1.employees.salary)
#
# DC 2: no two employees in the same dept can have different salaries
#   Expected violations: {1,3},{2,3},{4,6},{5,6}
¬(t0.employees.dept == t1.employees.dept ^ t0.employees.salary <> t1.employees.salary)
